# Production Backend Environment Variables
# IMPORTANT: Update all values below before deploying!

NODE_ENV=production
PORT=5002

# Production Database Configuration
# Replace with your actual VPS database credentials
DB_HOST=localhost
DB_USER=your_production_db_user
DB_PASSWORD=your_strong_db_password_here
DB_NAME=db_news

# JWT Secret - MUST be changed to a strong random string!
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_IN_PRODUCTION

# CORS Origin - Your production frontend URL
# Example: https://yourdomain.com or https://www.yourdomain.com
CORS_ORIGIN=https://your-production-domain.com
